machine:
  node:
    version: 0.10.28

dependencies:
  cache_directories:
    - "node_modules"
  override:
    - npm install --dev

deployment:
  staging:
    branch: master
    commands:
      - ./node_modules/.bin/gulp build